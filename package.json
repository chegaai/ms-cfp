{
  "name": "ms-cfp",
  "version": "1.0.0",
  "description": "chega.ai call for papers miscroservice",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clear": "rm -rf dist && rm -rf tsconfig.tsbuildinfo",
    "build:clean": "npm run clear && npm run build",
    "test": "exit 0"
  },
  "contributors": [
    "Rogério Munhoz <roz@rjmunhoz.me>"
  ],
  "license": "GPL-3.0",
  "devDependencies": {
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "@expresso/app": "^1.1.1",
    "@expresso/errors": "^1.3.0",
    "@expresso/server": "^1.1.3",
    "@expresso/validator": "^1.0.5"
  }
}
